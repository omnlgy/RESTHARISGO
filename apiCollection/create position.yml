info:
  name: create position
  type: http
  seq: 5.1

http:
  method: POST
  url: http://localhost:8080/api/positions/
  body:
    type: json
    data: |-
      {
        "title": "DevOps Engineer",
        "baseSalary": 12000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
