info:
  name: update department
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/departments/11
  body:
    type: json
    data: |-
      {
        "name": "tes(edited)",
        "code": "t-123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
