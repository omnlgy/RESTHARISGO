info:
  name: record attendance
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/api/attendance
  body:
    type: json
    data: |-
      {
        "employee_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
