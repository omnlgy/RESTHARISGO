info:
  name: update position
  type: http
  seq: 5.2

http:
  method: PUT
  url: http://localhost:8080/api/positions/7
  body:
    type: json
    data: |-
      {
        "title": "DevOps Senior",
        "baseSalary": 15000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
