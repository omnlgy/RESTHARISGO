info:
  name: delete position
  type: http
  seq: 5.3

http:
  method: DELETE
  url: http://localhost:8080/api/positions/7
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
