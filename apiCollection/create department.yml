info:
  name: create department
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/departments/
  body:
    type: json
    data: |-
      {
        "name": "tes",
        "code": "t-123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
